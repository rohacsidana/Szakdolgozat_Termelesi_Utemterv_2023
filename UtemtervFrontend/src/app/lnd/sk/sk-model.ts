export class Sk {
    public lnd_line: string;
    public lnd_part: number;
    public lnd_rate: number;

    constructor(lnd_line: string, lnd_part: number, lnd_rate: number) {
        this.lnd_line = lnd_line
        this.lnd_part = lnd_part
        this.lnd_rate = lnd_rate
    }
}
